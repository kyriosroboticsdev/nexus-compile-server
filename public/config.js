// Nexus Configuration
// The Google Client ID is safe to be public — it is secured by the
// list of Authorised JavaScript Origins in Google Cloud Console,
// not by being kept secret.
//
// The RobotEvents API token is entered by each user via Settings (⚙)
// and stored only in their own browser's localStorage. It is never
// stored in any file or committed to GitHub.

const NEXUS_CONFIG = {
  googleClientId: "937251908005-hvco8m4dpidqiuo09er1tec3p14426au.apps.googleusercontent.com",
};
